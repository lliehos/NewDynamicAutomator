# پلن اجرای ۳۸ کار (دوزبانه‌سازی، پالت رنگ، داشبورد، دیاگرام، منابع)

> **ایجاد:** 2026-09-25 · **وضعیت:** در حال اجرا

---

## قواعد اجرا (الزامی — بدون استثنا)

این قواعد را کارفرما تعیین کرده و در تمام مراحل باید رعایت شود:

1. **هر کار جداگانه** انجام شود و **قبل از رفتن به کار بعدی** کامل شود.
2. برای **هر کار** بیلد گرفته شود.
3. برای **هر کار** تست کاربری (اجرای واقعی روی اپ / هارنس) انجام شود.
4. اگر مشکل نبود → **کامیت مستقل** و سپس **پوش مستقل** برای همان کار.
5. **هیچ سؤالی پرسیده نشود.** اگر ابهام بود، محتمل‌ترین تفسیر انتخاب و اجرا شود.
6. **هیچ گزارشی داده نشود.** خروجی هر کار فقط یک خط: `#<شماره> <done|blocked> <hash>`
7. در صورت بروز مشکل و بلاک شدن، همان یک خط با `blocked` ثبت شود و به کار بعدی برود.

### قواعد کیفیت (تأکید کارفرما)

- **دوزبانه بودن (i18n):** هر لیبل/متن جدید باید **هم در `fa.json` و هم در `en.json`** اضافه شود.
  هیچ متن hard-code فارسی یا انگلیسی در ویو/JS نماند (به‌جز موارد فنی).
- **انکدینگ فارسی:** همه فایل‌ها **UTF-8 (without BOM)** باشند. نوشتن فایل با PowerShell فقط
  با `-Encoding utf8` مجاز است. کاراکتر `ی`/`ک` فارسی و نیم‌فاصله (ZWNJ = `\u200c`) حفظ شود.
  پس از هر ویرایش، JSONها با `ConvertFrom-Json` اعتبارسنجی شوند و متن فارسی در مرورگر
  بازبینی شود (نه فقط در ترمینال، چون ترمینال متن فارسی را خراب نشان می‌دهد).
- **پالت رنگ:** هر رنگی که در وب‌اپ استفاده می‌شود باید از پالت بیاید و ادمین بتواند تنظیمش کند.

---

## جدول وضعیت

| # | عنوان کوتاه | وضعیت | کامیت |
|---|-------------|--------|-------|
| 1 | در نظر گرفتن زبان در همه لیبل‌ها حتی افزونه‌ها | ✅ done | `592d169` |
| 2 | رنگ نارنجی هشدار لایسنس زیر ۱ ماه | ✅ done | `c835ed1` |
| 3 | اعمال فونت داخل input ها در پنل ادمین | ✅ done | `2c59e90` |
| 4 | همه رنگها از پالت + قابل تنظیم توسط ادمین | ✅ done | `521acf9` |
| 5 | چیدمان داشبورد: هم‌ترازی باکس‌ها، جای دسترسی سریع و رویدادهای اخیر | done | `6f0d392` |
| 6 | آمار و نمودارهای بیشتر در داشبورد ادمین | done | `d33bb5b` |
| 7 | بازبینی باکس Updates: فیلدهای بی‌معنی، عنوان دوزبانه، آپلود آفلاین | done | `a43814b` |
| 8 | ریسایز و هم‌ترازی باکس‌های صفحه لایسنس + تغییر عنوان | done | `d579c26` |
| 9 | باکس هویت برندینگ: فیلدهای بیشتر + دوزبانه + تکمیل پالت | ✅ done | `bfb6f3c` |
| 10 | انتقال خروج/ورود پنل از سایدبار به نوار بالا | ✅ done | `245c9a6` |
| 11 | چند قالب رنگی آماده + گزینه دستی | ✅ done | `4c30417` |
| 12 | لینک ویجت از فایل لایسنس + پارامتر در اپ ویندوزی | done | `3f4d3af` |
| 13 | آیکون اجرا/توقف فرآیندها در پنل ادمین (پلی چشمک‌زن / پاوز قرمز) | done | `d58e26d` |
| 14 | انواع احراز هویت (کاربران سیستم / LDAP) با تنظیمات و تب لاگین | done | `04ce412` |
| 15 | ذخیره تاریخچه تغییرات پنل ادمین (آخرین تغییردهنده + تاریخ) | done | `52e0222` |
| 16 | سوئیچ فعال‌سازی تنظیم تک‌تک رنگ‌های دیاگرام | done | `47e5b4f` |
| 17 | منوی جداگانه تنظیمات دیاگرام (رنگ/پهنای خط سلکتور، ignorePlayError و …) | done | `47e5b4f` |
| 18 | دریافت اندیس شروع/پایان تکرار + ذخیره در کش مرورگر | done | `e393ef5` |
| 19 | اعمال فوری تغییر نام منبع (لیبل + سرور) | ✅ done | `0101814` |
| 20 | نام فایل دانلود اکسل = نام منبع | ✅ done | `bbedf92` |
| 21 | کپی فرآیند در سرور (نه مرورگر) | not-started | |
| 22 | تغییر/بارگذاری مجدد محتوای منبع + بررسی سازگاری ستون‌های درگیر | not-started | |
| 23 | ویرایش اینلاین سلول + کانتکس منوی افزودن ردیف/ستون در مدال منابع | not-started | |
| 24 | سلکتور بهجای input معمولی: textarea با ریسایز خودکار | ✅ done | `f0b52ab` |
| 25 | سوئیچ «اجازه تغییر اطلاعات منبع» در اشتراک‌گذاری فرآیند | not-started | |
| 26 | راست‌چین/چپ‌چین شدن فیلدهای تنظیمات بر اساس نوع ورودی | done | `61f751f` |
| 27 | لاگ کردن متن سلکتور پویا توسط پلیر | ✅ done | `074b3a0` |
| 28 | کاهش پهنای باکسهای تنظیمات سامانه برای قرارگیری کنار هم | ✅ done | `d7e3f3c` |
| 29 | بهبود ظاهر صفحه OfflineUpdate (خصوصاً بخش آپلود) | done | `15519bd` |
| 30 | رفع خطای `duplicate id da-selector-parent` در افزونه | ✅ done | `9d3894e` |
| 31 | رفع نمایش `?` به‌جای کاراکترهای فارسی (انکدینگ) | ✅ done | `a7e6525` + `592d169` |
| 32 | رفع ایجاد دو نود «شروع» در ریشه بعد از رکورد | done | `f35ef93` |
| 33 | نمایش زمان/کاربر آخرین اجرا + آیکون تاریخچه اجراها | done | `cf995fb` |
| 34 | قابلیت جابجایی FAB افزونه رکوردر توسط کاربر | done | `5c735c9` |
| 35 | حذف/توضیح اکشن‌های تکراری + توضیح خاکستری زیر انتخاب اکشن | done | `a89cdcc` |
| 36 | نادیده گرفتن نودهایی که در توالی از نود شروع نیستند | done | `533337b` |
| 37 | شناسایی و هشدار حلقه‌های بی‌نهایت | done | `0718c85` |
| 38 | ایجاد شرط توسط رکوردر (Ctrl+Shift+کلیک) + راهنمای روی صفحه | done | `b975ee2` |

---

## یادداشت‌های فنی (پیش از هر کار بررسی شود)

### قواعد عمومی این پروژه (از حافظهٔ قبلی)

- **UI دوزبانه:** کلیدها در `src/Morobot.Web/wwwroot/locales/{fa,en}.json`. کلید تکراری در
  یک بلوک، بلوک قبلی را سایه می‌اندازد → کلید جدید باید **داخل بلوک موجود** ادغام شود.
- **چهار layout مستقل:** Admin `_Layout`، Panel `_Layout`، `Areas/Panel/Views/Tasks/Editor.cshtml`
  (Layout=null)، `Areas/Panel/Views/Shared/_AuthLayout.cshtml`. هر asset باید در همه اضافه شود.
- **`?v=` asset ها:** پس از ویرایش JS/CSS که قبلاً بارگذاری شده، نسخه را bump کنید.
- **افزونه:** موتور اجرا در `extension-global/player/engine.js`؛ loader در `background.js`
  با `importScripts`. جدول پیام دوزبانه **داخل خود engine.js** (`ENGINE_MSG` با کلیدهای `fa`
  و `en`) — نه در locale وب. افزونه `content/*.js` از `lib/ext-i18n.js` استفاده می‌کند.
- **`_Avatar`** در `Views/Shared` ریشه است (نه داخل Area).
- **مهاجرت EF** بدون `.Designer.cs` بی‌صدا نادیده گرفته می‌شود.
- **تست:** سرور `http://localhost:7200`؛ ورود Panel با `pro`/`Pro123!`؛ ادمین `admin`/`Admin123!`.
- **PowerShell:** دستور چندخطی inline خراب می‌شود → اسکریپت در `_run/*.ps1` بنویسید و اجرا کنید.

### باگ شناسایی‌شدهٔ ویرایشگر (خارج از ۳۸ کار) — fetch نودهای فرآیند

**وضعیت:** شناسایی و حل شد.

**نشانه:** باز کردن `/Panel/Tasks/Editor/9` روی فرآیندی که در دیتابیس ۲۱ نود دارد، بوم را با
**یک نود خالی** نشان می‌دهد؛ عنوان «فرآیند بدون عنوان» و شمارنده‌های داشبورد «۰ اقدام · ۰ گروه»
می‌شوند. اگر کاربر در همین حال ذخیره کند، گراف واقعی **بازنویسی و از دست می‌رود**.

**ریشه:** پاسخ `GET /api/tasks/{id}/canvas` یک **پاکت** است:

```json
{ "title": "...", "graphJson": "{\"nodes\":[...]}", "dataSources": [...], "updatedAtUtc": "...", "taskId": 9 }
```

`nodes` در **ریشهٔ** پاسخ نیست و داخل رشتهٔ `graphJson` قفل شده است. اما کلاینت در
`flow.js → load()` فقط `data.nodes || data.Nodes` را می‌خواند؛ چون پیدا نمی‌کند،
`emptyShell()` اجرا می‌شود و یک گراف تک‌نودی می‌سازد. `GraphJsonHelper.CountNodes` هم روی همان
ریشه کار می‌کند و ۰ برمی‌گرداند (پس شمارنده‌های داشبورد هم اشتباه‌اند).

**چرا خطرناک است:** ویرایشگر «موفق» بارگذاری می‌شود و نسخهٔ خالی را معتبر می‌داند؛ اولین ذخیره
داده را نابود می‌کند. هیچ خطایی روی صفحه دیده نمی‌شود.

**درمان:** `TaskService.GetCanvasAsync` باید پاکت را باز کند و **گراف** را برگرداند (نه پاکت).
اگر مقدار ذخیره‌شده پاکت بود، `graphJson` درونش پارس و همان برگردانده شود؛ وگرنه خود مقدار.

**درس برای بقیهٔ کارها:** برای کارهای ویرایشگر (مثل #۲۳) به‌جای کلیک روی صفحه، رفتار را با
تست واحد روی توابع واقعی `flow.js` (بیرون‌کشیدن با تطبیق آکولاد) بسنجید — صفحه در این محیط
نودها را بار نمی‌کند و هر ذخیره‌ای داده را خراب می‌کند.

### هشدار نوشتن با sqlcmd (تکرار نشود)

- `sqlcmd -Q` با مقدار طولانی (> چند KB) **بریده** می‌شود → گراف ناقص ذخیره می‌شود.
- `sqlcmd -i` فایل اسکریپت را ANSI می‌خواند و متن فارسی را `?` می‌کند (حتی با BOM).
- راه امن برای دادهٔ یونیکد و طولانی: فایل JSON در `wwwroot` بگذارید و از **خود API**
  (`PUT /api/tasks/{id}/canvas`) استفاده کنید.

### کلیدهای موجود پالت رنگ برند

`SystemSettingKeys`: `BrandColorPrimary`, `BrandColorPrimaryDark`, `BrandColorPrimaryLight`,
`BrandColorAccent`, `BrandColorSoft`, `BrandColorSoft2`, `BrandColorInk`, `BrandColorBorderSubtle`.
کارهای #4، #11، #16، #17 همه به این مجموعه و `BrandingService`/`BrandPaletteDefaults` وصل‌اند
→ باید با هم و هماهنگ طراحی شوند.

### رنگ سخت‌کد‌شدهٔ مهم که در #13 لازم است

`DEFAULT_HIGHLIGHT_COLOR = "#ea5455"` در `flow.js` و `engine.js` (رنگ قرمز پالت).

### یافتهٔ #31 — ریشهٔ نمایش `?` (تکرار نشود)

- ریشهٔ اصلی: **UTF-8 BOM** در سه فایل افزونه بود — `extension-global/manifest.json`,
  `extension-global/popup.html`, `extension-global/popup-selector.html`. کروم خودش
  `manifest.json` را پارس می‌کند و BOM ابتدای فایل باعث می‌شود متن غیر-ASCII (نام/توضیح فارسی)
  در UI افزونه‌ها `?` نشان داده شود.
- در کل مخزن ۴۷ فایل BOM داشتند (بقیه: مهاجرت‌های EF، چند `.cshtml`/`.js`/`.json`) → همه حذف شدند.
- **`.editorconfig`** در ریشه اضافه شد با `charset = utf-8` (هرگز `utf-8-bom`) تا برنگردد.
- اسکریپت‌های `_run/strip-bom.ps1` و `_run/strip-bom-all.ps1`: خواندن بایت + decode **strict**
  (`UTF8Encoding($false,$true)`) تا فایل نامعتبر به‌جای خرابی بی‌صدا، خطا بدهد.
- بررسی: هر ۳ فایل `BOM=false` و `validJson=True`؛ متن فارسی و نیم‌فاصله سالم (۳۱/۱۴۵/۱۵۰ کاراکتر).

> **نکته برای افزونهٔ رکوردر (`extension-smart-recorder`):** این افزونه **هیچ استفاده‌ای از
> i18n ندارد** (صفر ارجاع به `lib/ext-i18n.js`) و متن‌هایش hard-code است. اگر `?` در آن دیده
> شود، ریشهٔ انکدینگ نیست بلکه **نبود i18n** است → مربوط به کار #1.

### سه ریشهٔ نمایش `?` که در #31 برطرف شد (تکرار نشود)

۱. **BOM (فایل):** `extension-global/manifest.json` + `popup.html` + `popup-selector.html`.
   اسکریپت‌ها: `_run/strip-bom.ps1` و `_run/strip-bom-all.ps1`.
۲. **دادهٔ خراب قدیمی (دیتابیس):** `DataSources` id=9 با `???? ?????` (بایت `3F 00` واقعی،
   نه مشکل نمایش). نام اصلی `کارکرد های Adrise` بود (از `FileName` و سه ردیف ۵/۶/۸ هم‌نام
   بازسازی شد). همان عنوان در `Processes.GraphJson` id=9 هم منعکس شده بود ← عنوان `dataSources`
   و عنوان نودهایی که رکوردر نوشته بود (`????` ← شروع، `???? Starred` ← ستاره‌دار Starred،
   `????? 1` ← انتظار 1). اسکریپت: `_run/repair-corrupt.ps1` + `_run/fix-node-titles.ps1`.
۳. **کش کلاینت:** داشبورد از گراف کش‌شده (localStorage) می‌خواند، پس بعد از ترمیم سرور هنوز
   `????` نشان می‌داد. اضافه شد: `refreshServerGraphs()` در `local-dashboard.js` که canvas را
   از سرور می‌کشد و فقط وقتی لیست منابع تغییر کرد دوباره رندر می‌کند (`?v=mrbt-3`).

**مسیر نوشتن فعلی پاک است:** همهٔ fetch های وب `Content-Type: application/json` (UTF-8) و همهٔ
کدهای سرور `Encoding.UTF8` هستند؛ تست رفت‌وبرگشت با «منبع آزمون فارسی ۱۲۳» در DB درست ذخیره شد.
پس این کار پاک‌سازی خرابی ذخیره‌شده بود، نه تغییر روال.

### یافتهٔ #1 — زبان در افزونه‌ها

- **ریشهٔ اصلی:** `extension-smart-recorder` **هیچ i18n نداشت** — `popup.html` با `lang="fa" dir="rtl"`
  ولی متن‌های hard-code انگلیسی (`Refresh`, `Stop thinking`)، و `content/portal-ui.js` متن hard-code
  **فارسی**. یعنیکاربر انگلیسی‌زبان فارسی می‌دید و کاربر فارسی‌زبان انگلیسی.
- **راه‌حل:**
  - `extension-smart-recorder/lib/rec-i18n.js` (جدید) — همان الگوی `ext-i18n.js` با کلید `uiCulture`.
  - `popup.html`/`popup.js` با `data-i18n` + `lib/rec-i18n.js`.
  - `content/fab.js` و `content/portal-ui.js` ترجمه شدند (در لیست `content_scripts` و در
    `chrome.scripting.executeScript` فایل `lib/rec-i18n.js` اول اضافه شد).
  - `content/portal-bridge.js` حالا `uiCulture` را از کوکی/`localStorage`‌ی `da_culture` منتشر می‌کند
    و به `da:locale` و `storage` گوش می‌دهد — قبلاً هیچ‌وقت فرهنگ را push نمی‌کرد.
  - **`_locales/{fa,en}/messages.json`** در **هر دو افزونه** + `default_locale: "fa"` و
    `name`/`description`/`default_title` با `__MSG_...__` — تا نام/توضیح افزونه در UI کروم هم
    با زبان مرورگر عوض شود (قبلاً ثابت بود).
- **اعتبارسنجی:** `node --check` روی ۵ اسکریپت OK؛ JSON همه OK؛ `rec-i18n` پاریته fa/en = ۱۶/۱۶؛
  `_locales` پاریته = ۳/۳ در هر دو؛ هیچ متن فارسی hard-code در فایل‌های UI رکوردر باقی نماند؛
  `ENGINE_MSG` موتور = ۵۹/۵۹ (قبلاً سالم بود).
- **تست:** افزونه در این محیط نصب نمی‌شود → بررسی ایستا + راه‌اندازی اپ (`localhost:7200`) و
  تأیید اینکه `da_culture`/`da:locale` توسط `i18n.js` منتشر می‌شود.

---

## ترتیب پیشنهادی اجرا

از کم‌ریسک/کوچک به سنگین، با توجه به وابستگی‌ها:

1. **اصلاحات سریع و مستقل:** #31، #30، #20، #19، #28، #27، #10، #2، #3
2. **دیاگرام و ویرایشگر:** #24، #26، #35، #36، #37، #32، #16، #17، #18
3. **منابع:** #22، #23، #25، #21، #33
4. **پالت رنگ (زنجیره‌ای):** #4 → #11 → #9 (چون #11 و #9 روی #4 سوار می‌شوند)
5. **داشبورد:** #5 → #6 → #13
6. **تنظیمات و لایسنس:** #7، #8، #29، #15، #12
7. **احراز هویت:** #14 (سنگین — migration + UI + ورود)
8. **افزونه:** #1، #34، #38

---

## نکته دربارهٔ اجرای افزونه‌ها (#1، #30، #31، #34، #38)

افزونه در مرورگر کاربر نصب می‌شود و **از این محیط قابل اجرای واقعی نیست**؛ تست این موارد
با اصلاح کد + بررسی ایستا (syntax + بازبینی منطق) انجام می‌شود و در فایل پلن ثبت می‌گردد.

---

## جزئیات اجرایی ۲۸ کار باقی‌مانده

هر بند: **محل تغییر** + **تعریف «انجام‌شده»** + **تست**. هیچ ابهامی نباید بماند.

### زنجیرهٔ پالت رنگ (وابسته — به همین ترتیب)

**#4 — همهٔ رنگ‌ها از پالت + قابل تنظیم توسط ادمین**
- محل: `SystemSettingKeys`، `BrandingService`، `admin-app.css`، `Areas/Panel/Views/Shared/_Layout.cshtml`، `flow.js`، `engine.js`.
- کار: هر رنگ سخت‌کد‌شده در پوستهٔ ادمین و پنل شناسایی و به متغیر CSS (`--brand-*`) تبدیل شود.
  `BrandingService` این متغیرها را از `SystemSettingKeys` پر کند. `DEFAULT_HIGHLIGHT_COLOR = "#ea5455"`
  در `flow.js` و `engine.js` از پالت خوانده شود.
- انجام‌شده: هیچ hex سخت‌کدی جز مقادیر پیش‌فرض پالت در CSS/JS نماند؛ تغییر مقدار در ادمین رنگ صفحه را عوض کند.
- تست: مقدار `BrandColorPrimary` را در ادمین عوض کن → رنگ دکمه‌ها/لینک‌ها در هر دو پنل و ادمین عوض شود.

**#11 — چند قالب رنگی آماده + گزینهٔ دستی**
- محل: `_locales`/`locales` (fa/en)، صفحهٔ Branding ادمین، `BrandingService`.
- کار: ۴ تا ۶ تم آماده (مثلاً teal/indigo/rose/amber/slate) که یک‌جا کل پالت را ست کند + گزینهٔ «دستی» برای تک‌تک رنگ‌ها.
- انجام‌شده: انتخاب تم همهٔ `--brand-*` را یک‌جا به‌روز کند؛ «دستی» فیلدهای تک‌رنگ را نشان دهد.
- تست: هر تم را انتخاب کن → رنگ‌ها عوض شوند؛ «دستی» → فقط رنگ انتخابی عوض شود.

**#9 — باکس هویت برندینگ: فیلدهای بیشتر + دوزبانه + تکمیل پالت**
- محل: `Areas/Admin/Views/Branding/Index.cshtml`، `SystemSettingKeys`، مدل فرم.
- کار: برای هر فیلد متنی موجود، جفت **Fa/En** اضافه شود (`LabelFa`/`LabelEn`, `HintFa`/`HintEn` الگو).
  همهٔ کلیدهای پالت #4 در این باکس نمایش داده شوند. عنوان/توضیح دوزبانه شوند.
- انجام‌شده: هر فیلد برندینگ دو مقدار داشته باشد؛ در حالت `en` مقدار En و در `fa` مقدار Fa استفاده شود.
- تست: زبان را عوض کن → برچسب‌ها و مقادیر پیش‌فرض عوض شوند؛ همهٔ رنگ‌های پالت قابل ویرایش باشند.

### دیاگرام و ویرایشگر

**#24 — سلکتور با textarea و ریسایز خودکار**
- محل: `flow.js` → `conditionInspectorHtml`، `stepInspectorHtml`، `groupInspectorHtml` (فیلد سلکتور).
- کار: هرجا سلکتور وارد می‌شود، `<input>` → `<textarea rows="1">` با auto-grow (بر اساس `scrollHeight`).
- انجام‌شده: سلکتورهای بلند کامل دیده شوند؛ ارتفاع با محتوا رشد کند.
- تست: یک سلکتور ۳ خطی وارد کن → textarea رشد کند و مقدار ذخیره/بارگذاری شود.

**#26 — راست‌چین/چپ‌چین بر اساس نوع ورودی**
- محل: CSS ادمین + `flow.js` (فیلدها).
- کار: فیلدهای سلکتور/آدرس/کد LTR شوند (`dir="ltr"`)، فیلدهای متن فارسی RTL بمانند. با یک کلاس/اتریبیوت `data-input-kind`.
- انجام‌شده: سلکتور در حالت RTL هم چپ‌چین نمایش داده شود.
- تست: زبان fa → سلکتور LTR، عنوان RTL.

**#35 — حذف/توضیح اکشن‌های تکراری + توضیح خاکستری زیر انتخاب اکشن**
- محل: `flow.js` → لیست `ACTION_TYPES`/`recordedActionVerbFa`، گزینه‌های انتخاب اکشن.
- کار: اکشن‌های هم‌معنا یکی شوند (یا در UI با برچسب یکی شوند) و زیر هر گزینه یک `<p class="palette-hint">` توضیح بیاید.
- انجام‌شده: هیچ دو اکشنی با کار یکسان در لیست نباشد؛ هر گزینه یک توضیح تک‌خطی داشته باشد.
- تست: منوی اکشن را باز کن → توضیح‌ها دیده شوند.

**#36 — نادیده گرفتن نودهایی که در توالی از نود شروع نیستند**
- محل: `engine.js` → `collectPlaySteps`/`executeFlow`، و `flow.js` → اعتبارسنجی.
- کار: نودهای یتیم (که از `start` قابل دسترسی نیستند) در اجرا نادیده گرفته شوند و در ویرایشگر با هشدار خاکستری علامت‌گذاری شوند.
- انجام‌شده: اجرا فقط نودهای متصل به `start` را اجرا کند.
- تست: یک نود جدا روی بوم بگذار → اجرا آن را رد کند و هشدار نشان داده شود.

**#37 — شناسایی و هشدار حلقه‌های بی‌نهایت**
- محل: `flow.js` (اعتبارسنجی زمان طراحی) + `engine.js` (`resolveLoopBackLimit` موجود).
- کار: در ویرایشگر، اگر چرخه‌ای بدون شرط خروج باشد هشدار بده. (موتور قبلاً سقف `loopBackLimit` دارد.)
- انجام‌شده: بوم چرخهٔ بی‌شرط را هشدار دهد.
- تست: `A → B → A` بدون شرط بکش → هشدار ظاهر شود.

**#32 — رفع ایجاد دو نود «شروع» در ریشه بعد از رکورد**
- محل: `flow.js` (ایمپورت گراف رکوردر) و/یا `engine.js`/`background.js` (خروجی رکوردر).
- کار: ریشه فقط یک `kind:"start"` داشته باشد؛ هنگام merge، شروع تکراری ادغام/حذف شود.
- انجام‌شده: بعد از ضبط، دقیقاً یک نود شروع در ریشه باشد.
- تست: گرافی با دو start را وارد کن → فقط یکی بماند.

**#16 — سوئیچ فعال‌سازی تک‌تک رنگ‌های دیاگرام**
- محل: `SystemSettingKeys` + صفحهٔ تنظیمات دیاگرام (از #17) + `flow.js`.
- کار: برای هر رنگ دیاگرام یک سوئیچ روشن/خاموش، تا ادمین بتواند پالت را نادیده بگیرد بدون حذف مقدار.
- انجام‌شده: خاموش‌کردن سوئیچ → رنگ پیش‌فرض دیاگرام اعمال شود.
- تست: سوئیچ یک رنگ را خاموش کن → مقدار قبلی بماند ولی اعمال نشود.

**#17 — منوی جداگانهٔ تنظیمات دیاگرام**
- محل: متد جدید در `SystemSettingKeys` (گروه جدید) + ویو/کامپوننت تنظیمات + `flow.js`/`engine.js`.
- کار: گروه «دیاگرام» شامل: رنگ و پهنای خط سلکتور، `ignorePlayError`، فاصلهٔ مراحل، `loopBackLimit`.
- انجام‌شده: این مقادیر از سرور خوانده و به‌عنوان پیش‌فرض گراف اعمال شوند.
- تست: مقدار را در ادمین عوض کن → گراف جدید با همان پیش‌فرض باز شود.

**#18 — دریافت اندیس شروع/پایان تکرار + ذخیره در کش مرورگر**
- محل: `engine.js` (`resolveProcessIterations`)، `flow.js`، `localStorage`/`DaSecureStore`.
- کار: کاربر بتواند محدودهٔ ردیف‌ها را تعیین کند (start/end) و در کش مرورگر برای همان فرآیند بماند.
- انجام‌شده: تنظیم محدوده در UI، اعمال در اجرا، ماندگاری بعد از رفرش.
- تست: محدوده ۵..۱۰ بگذار → رفرش کن → همان محدوده بماند و اجرا از ۵ تا ۱۰ برود.

### منابع

**#22 — تغییر/بارگذاری مجدد محتوا + بررسی سازگاری ستون‌های درگیر**
- محل: `DataSourceService`، `TasksApiController` (`DataSourcesApiController`)، `local-data-sources.js`، `flow.js`.
- کار: بارگذاری فایل جدید روی منبع موجود + تشخیص ستون‌هایی که نودهای فرآیند به آن‌ها وابسته‌اند؛ اگر ستونی حذف شده، هشدار/خطا با ذکر نودها.
- انجام‌شده: بارگذاری مجدد منبعی که ستون `X` را حذف کرده → هشدار دقیق با نام نودها.
- تست: ستون وابسته را حذف کن → پیام هشدار با فهرست نودها.

**#23 — ویرایش اینلاین سلول + کانتکست منو**
- محل: `local-data-sources.js` (مدال منابع)، ویوی جدول.
- کار: دابل‌کلیک روی سلول → ویرایش اینلاین؛ راست‌کلیک روی هدر/سلول → منوی افزودن ردیف/ستون.
- انجام‌شده: ویرایش سلول بدون باز کردن فرم جدا ذخیره شود.
- تست: یک سلول را ویرایش کن → بعد از رفرش مقدار جدید بماند.

**#25 — سوئیچ «اجازه تغییر اطلاعات منبع» در اشتراک‌گذاری**
- محل: `ProcessShare` (entity + migration)، `task-share.js`، `EntitlementService`/چک دسترسی، `TasksApiController`.
- کار: هنگام اشتراک، سوئیچی مثل `CanChangeDataSource` ست شود و در API بررسی گردد.
- انجام‌شده: کاربر بدون این اجازه نتواند منبع را عوض کند (خطای روشن).
- تست: اشتراک بدون سوئیچ → تغییر منبع رد شود.

**#21 — کپی فرآیند در سرور**
- محل: `TasksApiController` (endpoint کپی)، `TaskService`، `local-tasks.js` (الان کپی کلاینت‌محور است).
- کار: endpoint سرور برای کپی کل گراف + منابع متصل با نام «(کپی)».
- انجام‌شده: کپی از دکمهٔ پنل → رکورد جدید روی سرور ساخته شود (نه فقط localStorage).
- تست: کپی کن → در `/api/tasks` رکورد دوم دیده شود.

**#33 — نمایش زمان/کاربر آخرین اجرا + آیکون تاریخچه**
- محل: `PlaySessionTracker`/`EventLogs`، فهرست فرآیندها (`local-tasks.js`)، `TasksApiController` (فهرست).
- کار: در هر ردیف فرآیند، «آخرین اجرا: تاریخ/کاربر» + آیکون کلیک‌پذیر به تاریخچهٔ اجراهای آن فرآیند.
- انجام‌شده: بعد از یک اجرا، فهرست زمان و کاربر را نشان دهد.
- تست: یک فرآیند را اجرا کن → زمان/کاربر به‌روز شود و آیکون تاریخچه باز شود.

### داشبورد ادمین

**#5 — چیدمان: هم‌ترازی، جای دسترسی سریع و رویدادهای اخیر**
- محل: `Areas/Admin/Views/Home/Index.cshtml` + `admin-app.css`.
- کار: باکس‌های آمار هم‌تراز با لبهٔ کارت؛ «دسترسی سریع» تمام‌عرض زیر آمار؛ «رویدادهای اخیر» جای قبلی دسترسی سریع.
- انجام‌شده: آمار در یک ردیف هم‌عرض؛ دسترسی سریع زیر آن؛ رویدادها پایین‌تر.
- تست: اسکرین‌شات — باکس‌ها هم‌تراز، چیدمان جدید درست.

**#6 — آمار و نمودارهای بیشتر**
- محل: `admin-charts.js`، همان ویو.
- کار: نمودارهای اضافه (روند اجراها، توزیع پلن‌ها، کاربران فعال) با دادهٔ واقعی سرور.
- انجام‌شده: حداقل دو نمودار جدید با دادهٔ واقعی (نه نمونهٔ ثابت).
- تست: اعداد نمودار با DB بخواند.

**#13 — آیکون اجرا/توقف با پلی چشمک‌زن و پاوز قرمز**
- محل: فهرست فرآیندهای ادمین (`admin-processes.js`) + CSS انیمیشن.
- کار: وضعیت زندهٔ اجرا از SignalR (`PlayDataHub`)؛ در حال اجرا = پلی چشمک‌زن، متوقف = پاوز قرمز.
- انجام‌شده: وضعیت با رویداد زندهٔ اجرا عوض شود.
- تست: اجرا را از پنل شروع کن → آیکون ادمین چشمک بزند.

### تنظیمات و لایسنس

**#7 — بازبینی باکس Updates**
- محل: گروه Updates در `SystemSettingKeys` + ویو تنظیمات.
- کار: فیلدهای بی‌معنی حذف؛ فیلدهای اطلاعاتی فقط‌خواندنی/لیبل (نه input خالی)؛ عنوان دوزبانه؛ آپلود فایل آپدیت آفلاین (به #29 وصل).
- انجام‌شده: هیچ input خالی/بی‌استفاده؛ عنوان fa/en؛ آپلود کار کند.
- تست: بارگذاری بسته → وضعیت به‌روز شود.

**#8 — ریسایز و هم‌ترازی باکس‌های لایسنس + تغییر عنوان**
- محل: `Areas/Admin/Views/License/Index.cshtml` + CSS.
- کار: «وضعیت لایسنس» پهن‌تر؛ «درخواست فعال‌سازی» و «آپدیت نرم‌افزار» زیر هم؛ عنوان «دریافت لایسنس جدید» → **«اعمال لایسنس جدید»** (fa/en).
- انجام‌شده: چیدمان سه‌باکسی هم‌تراز؛ عنوان جدید در هر دو زبان.
- تست: اسکرین‌شات + بررسی کلید i18n.

**#29 — بهبود ظاهر صفحهٔ OfflineUpdate**
- محل: `Areas/Admin/Views/OfflineUpdate/Index.cshtml` + CSS.
- کار: باکس آپلود برجسته، کشیدن‌ورهاکردن فایل، وضعیت‌های واضح (آپلود/استیج/اعمال).
- انجام‌شده: بخش آپلود تمیز و واضح؛ پیام‌ها دوزبانه.
- تست: آپلود فایل نامعتبر → خطای روشن.

**#15 — تاریخچهٔ تغییرات پنل ادمین**
- محل: `EventLogs` (موجود) + میدل‌ور/فیلتر جدید + ستون‌های آخرین تغییردهنده/تاریخ روی نهادها (migration).
- کار: هر تغییر تنظیمات/برندینگ/پلن با کاربر و زمان ثبت شود و در لیست نشان داده شود.
- انجام‌شده: بعد از ویرایش، «آخرین ویرایش: کاربر/تاریخ» درست باشد.
- تست: یک تنظیم را عوض کن → مقدار کاربر/زمان ثبت شود.

**#12 — لینک ویجت از فایل لایسنس + پارامتر در اپ ویندوزی**
- محل: `Morobot.Licensing` (خواندن فیلد جدید)، `BrandingService`/تنظیمات، لینک در UI.
- کار: فیلد لینک ویجت از سند لایسنس خوانده و به اپ ویندوزی به‌صورت پارامتر پاس داده شود.
- انجام‌شده: لینک از لایسنس خوانده شود و در UI نمایش/استفاده شود.
- تست: سند بدون فیلد → رفتار پیش‌فرض امن (بدون لینک).

### احراز هویت

**#14 — انواع احراز هویت (کاربران سیستم / LDAP)**
- محل: migration جدید روی `Users`/تنظیمات، `AuthService`، صفحهٔ ورود `_AuthLayout`، گروه تنظیمات جدید.
- کار: حالت «محلی» و «کاربران سیستم/LDAP» با تنظیمات سرور (host/port/baseDN/فیلتر) و تب ورود مناسب؛ fallback محلی برای ادمین.
- انجام‌شده: با LDAP تنظیم‌شده، ورود از LDAP؛ در غیر این‌صورت محلی. کلمهٔ عبور محلی ادمین همیشه کار کند (قفل‌نشدن).
- تست: سویچ حالت → تب ورود عوض شود؛ ورود محلی ادمین در هر دو حالت کار کند.

### افزونه (فقط بررسی ایستا در این محیط)

**#34 — جابجایی FAB افزونهٔ رکوردر توسط کاربر**
- محل: `extension-smart-recorder/content/fab.js` + CSS.
- کار: FAB قابل درگ؛ موقعیت در `chrome.storage.local` ذخیره شود و بعد از بارگذاری بازیابی گردد.
- انجام‌شده: درگ + ذخیرهٔ موقعیت + بازیابی.
- تست: فقط `node --check` + بازبینی منطق (اجرای افزونه در این محیط ممکن نیست).

**#38 — ایجاد شرط با Ctrl+Shift+کلیک + راهنمای روی صفحه**
- محل: `extension-smart-recorder/content/capture.js`، `selector.js`، `background.js` (پیام ضبط شرط)، `flow.js` (ایجاد نود شرط).
- کار: ترکیب Ctrl+Shift+کلیک، عملیات شرط را ضبط کند؛ یک راهنمای کوچک روی صفحه ترکیب را نشان دهد.
- انجام‌شده: رویداد شرط ضبط و به‌شکل نود `condition` ذخیره شود؛ راهنما نمایش داده شود.
- تست: فقط بررسی ایستا + بازبینی منطق.

